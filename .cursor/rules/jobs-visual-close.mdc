---
description: 乔布斯必须自己看结果是否符合当前目标；不符合就分析、学习、修复并验证到过关，禁止用文档或 pytest 顶替看图。
alwaysApply: true
---

# 乔布斯看图闭环

用户已裁定：每次交付乔布斯必须自己对照**当前说好的目标**看结果。不符合就找资料、学技术、分析、动手修、再验证，直到符合。同意后这是长期规矩。

## 必须做

1. **先看用户会看的那张图/那一屏**，再写结论。数字（Δψ、pytest、coverage）不能代替看图。
2. 与目标不符时：**不停下来改 md、报「已验证」**。继续：归因 → 查资料/SOTA → 改算法或模型 → 出图/跑通 → 再看。直到过关或真实阻塞。
3. pytest 只证明函数契约。视觉目标（引导线、车道轨、牙子、障碍停笔）必须打开产物图核对门：轨是否司机看见的那两条边、蓝线是否夹死、有无喷噪/钩子/弦。
4. 文档只在**图已过门**之后补。未过门的纪要不算完成。

## 反面

- ❌ 图上有红框问题，却更新 auto_docs 并写 42 passed
- ❌ 自知不符合预期，把修复留成「下一步建议」
- ❌ 用平滑、裁剪、Δψ 掩盖错误边界

## 正面

- ✅ 看图 → 点名失败框 → 根因（哪根轨在哪一行跳了）→ 改代码 → 再出图 → 再看
- ✅ 阻塞时写清缺什么（权/真机/数据），旁路可做的继续做完
