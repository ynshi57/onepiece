-r requirements.txt
pytest
httpx
# Enables uvicorn --reload (auto-restart on code edits) used by
# start_diagnostics_platform.sh. Dev-only.
watchfiles
# numpy / Pillow are NOT listed here on purpose: app.main imports them
# transitively, so they live in requirements.txt (single source of truth) and
# are inherited via the -r above.
